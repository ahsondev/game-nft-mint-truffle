import { actionTypes } from 'utils/config'

export const getServiceConfig = () => (dispatch: any) => new Promise((resolve, reject) => {
  // getServiceConfigApi().then((res: any) => {
  //   dispatch({
  //     type: actionTypes.FETCH_SERVICE_CONFIG,
  //     payload: res.data
  //   })
  // }, err => {})
})
